const assert = require('assert');
const identifyVehicle = require('./');

describe(__filename, () => {
  it('Can identify a car', () => {
    const car = {
      wheels: 4,
      treads: false,
      engine: true,
    };
  });
});