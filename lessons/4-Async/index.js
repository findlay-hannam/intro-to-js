const { getFirstNumberFromServer, getSecondNumberFromServer } = require('./api');

// Here you need to write a function that makes two server calls, and adds the two responses together
async function addServerResponses() {}

module.exports = addServerResponses;